services:
  postgres:
    image: postgres:16
    container_name: sigrec-postgres
    environment:
      POSTGRES_DB: segrec
      POSTGRES_USER: jose
      POSTGRES_PASSWORD: Joseluiz1
      TZ: America/Sao_Paulo
    ports:
      - "5432:5432"
    volumes:
      - sigrec_pgdata:/var/lib/postgresql/data

volumes:
  sigrec_pgdata:
